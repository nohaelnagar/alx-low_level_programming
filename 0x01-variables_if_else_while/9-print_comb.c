#include <stdio.h>

/**
 * main - print all possible combinations of single-digit numbers
 * Return: Always 0 (success)
 */
int main(void)
{
	int n = 48;

	while (n <= 57)
	{
		putchar(n', ');
		n++;
	if (n == 57)
	{
		putchar(n);
	}
	}
	putchar('\n');
	return (0);
}
